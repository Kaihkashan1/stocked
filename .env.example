# Copy this file to .env and fill in the values.
#   cp .env.example .env

# Required — from https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional — a second Google account's own Gemini API key. The free
# tier's 20/day cap is per key/project, so once the first is used up,
# the app falls back to this one instead of failing for the rest of the
# day (doubles the effective cap to 40/day). Leave unset to use just one.
# GEMINI_API_KEY_2=

# Optional override. gemini-3.6-flash is the current free-tier workhorse
# for video + JSON. Check https://aistudio.google.com/rate-limit
GEMINI_MODEL=gemini-3.6-flash

# Required — the long id in the Sheet URL:
#   https://docs.google.com/spreadsheets/d/THIS_PART/edit
GOOGLE_SHEET_ID=

# Path to the JSON key you download for the Google Cloud service account.
GOOGLE_SERVICE_ACCOUNT_FILE=./service_account.json
# On Vercel, paste the JSON key as a single env var instead of a file:
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Shared secret the iPhone Shortcut sends as header X-Recipe-Box-Key.
# Pick any random string; keep it private.
RECIPE_BOX_SECRET=change-me

# Required for Instagram links — fetches posts/reels through a paid Apify
# actor. No Instagram account is ever authenticated, so this carries no
# risk of your account being flagged or restricted (see README). There is
# no cookie-based fallback — without this, Instagram links will fail.
APIFY_API_TOKEN=
# APIFY_INSTAGRAM_ACTOR=apify~instagram-post-scraper

# Optional — free push notifications via https://ntfy.sh
# Create a unique topic name and subscribe to it in the ntfy app.
# NTFY_TOPIC=recipe-box-yourname
